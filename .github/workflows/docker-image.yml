name: Docker Image CI
run-name: ${{ github.actor }} is testing docker ci
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag cmdstat:$(date +%s)